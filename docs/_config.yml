# GitHub Pages Jekyll configuration
# This file tells GitHub Pages not to use Jekyll processing
# since we're using plain HTML/CSS/JS

# Exclude files from processing
exclude:
  - README.md
  - node_modules/

# Include hidden files/folders if needed
include:
  - .nojekyll
